# Nano Editor Troubleshooting Guide

> 🏠 [Home](./README.md) > **📚 Documentation** > **Nano Troubleshooting**

## 🔧 Решение проблемы с ошибками nano в Claude Code

### Проблема: Ошибки конфигурации nano при запуске из Claude

#### Симптомы:
- При нажатии `Ctrl+G` в Claude Code nano запускается с ошибками
- Внизу экрана появляются сообщения об ошибках конфигурации

#### Причина:
Некоторые опции nano не поддерживаются в Alpine Linux версии, используемой в Docker контейнере.

---

## ✅ Решение

### 1. Обновленный Docker образ
Проблема исправлена в версии `glm-docker-tools:latest` (v1.1.0+).

### 2. Исправленная конфигурация nano
Неподдерживаемые опции удалены:
```bash
# ❌ Удалены (не поддерживаются в Alpine):
# set const
# set smooth
# set cut

# ✅ Оставлены (работают в Alpine):
set linenumbers
set mouse
set softwrap
set tabsize 4
set tabstospaces
set autoindent
```

---

## 🚀 Как проверить исправление

### Метод 1: Запустить новый контейнер
```bash
# Остановить старые контейнеры
docker ps -aq --filter "name=glm-docker" | xargs -r docker stop
docker ps -aq --filter "name=glm-docker" | xargs -r docker rm

# Запустить новый контейнер
./glm-launch.sh
```

### Метод 2: Тестировать nano напрямую
```bash
# Запустить тестовый контейнер
docker run -d --name test-nano \
  -v ~/.claude:/root/.claude \
  glm-docker-tools:latest \
  tail -f /dev/null

# Проверить nano
docker exec test-nano nano --version

# Проверить конфигурацию
docker exec test-nano cat /root/.nanorc
```

---

## 🎯 Правильное использование nano в Claude Code

### Команды для редактирования:
```bash
# В Claude Code попробуйте:
"Открой config.json в nano"
"Редактируй main.py с помощью nano"
"Измени settings.json"

# Или напрямую:
!nano filename.txt
```

### Горячие клавиши nano:
- `Ctrl+G` - Показать справку/помощь
- `Ctrl+X` - Выйти из nano
- `Ctrl+O` - Сохранить файл
- `Ctrl+W` - Поиск в файле
- `Ctrl+K` - Вырезать строку
- `Ctrl+U` - Вставить строку

---

## 🔍 Диагностика

### Проверить версию контейнера:
```bash
docker images | grep glm-docker-tools
# Должна быть версия с последним временем сборки
```

### Проверить конфигурацию:
```bash
docker exec <container-name> cat /root/.nanorc
# Не должно быть сообщений об ошибках
```

### Проверить переменные окружения:
```bash
docker exec <container-name> sh -c 'echo "EDITOR: $EDITOR"'
docker exec <container-name> sh -c 'echo "VISUAL: $VISUAL"'
# Должно быть: EDITOR: nano, VISUAL: nano
```

---

## 🚨 Если проблема осталась

### Полная переустановка:
```bash
# 1. Удалить все контейнеры
docker ps -aq --filter "name=glm-docker" | xargs -r docker rm

# 2. Удалить старый образ
docker rmi glm-docker-tools:latest

# 3. Пересобрать образ
docker build -t glm-docker-tools:latest .

# 4. Запустить новый контейнер
./glm-launch.sh
```

### Проверка логов:
```bash
# Проверить логи контейнера
docker logs <container-name>

# Запустить в отладочном режиме
docker run --rm -it glm-docker-tools:latest sh
```

---

## 📚 Связанная документация

- **[Nano Editor Setup](./docs/NANO_EDITOR_SETUP.md)** - Полное руководство по настройке
- **[Docker Configuration](./README.md#docker-setup)** - Настройка Docker окружения
- **[Troubleshooting](./SECURITY.md#troubleshooting)** - Общие решения проблем

---

**Статус**: ✅ **ИСПРАВЛЕНО** - Версия 1.1.0+ содержит исправленную конфигурацию nano