# GLM Docker Tools Configuration
# ==================================
# Copy this file to .env and customize for your environment
# IMPORTANT: .env is in .gitignore - never commit real secrets to git

# ============================================================================
# DOCKER IMAGE CONFIGURATION
# ============================================================================

# Docker image name (without tag)
# Default: glm-docker-tools
IMAGE_NAME=glm-docker-tools

# Docker image tag
# Default: latest
IMAGE_TAG=latest

# ============================================================================
# CONTAINER CONFIGURATION
# ============================================================================

# Container lifecycle mode: autodel, debug, nodebug
# - autodel: Auto-delete container after exit (default)
# - debug: Keep container and provide shell access for debugging
# - nodebug: Keep container but no shell access
# Default: autodel
CLAUDE_LAUNCH_MODE=autodel

# ============================================================================
# VOLUME MOUNTS
# ============================================================================

# Claude configuration directory
# Default: $HOME/.claude
CLAUDE_HOME=$HOME/.claude

# Workspace directory (mounted as /workspace in container)
# Default: current directory
WORKSPACE=$(pwd)

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Log level: INFO, WARNING, ERROR
# Default: INFO
CLAUDE_LOG_LEVEL=INFO

# Log format: text, json
# - text: Human-readable colored output (default)
# - json: Structured JSON logging for parsing
# Default: text
CLAUDE_LOG_FORMAT=text

# Log file path (optional)
# Empty = no file logging (stdout only)
# Example: /tmp/glm-launch.log
# Default: empty (no file logging)
CLAUDE_LOG_FILE=

# ============================================================================
# GLM API CONFIGURATION
# ============================================================================

# GLM API endpoint URL
# Default: https://api.z.ai/api/anthropic
GLM_API_ENDPOINT=https://api.z.ai/api/anthropic

# Default GLM model
# Options: glm-4.6, glm-4.5-air, glm-4-plus
# Default: glm-4.6
GLM_DEFAULT_MODEL=glm-4.6

# Haiku model override (for cost optimization)
# Default: glm-4.5-air
GLM_HAIKU_MODEL=glm-4.5-air

# ============================================================================
# RESOURCE LIMITS
# ============================================================================

# Container memory limit (e.g., 4g, 8g, 16g)
# Default: 4g
CONTAINER_MEMORY_LIMIT=4g

# Container CPU limit (e.g., 1.0, 2.0, 4.0)
# Default: 2.0
CONTAINER_CPU_LIMIT=2.0

# ============================================================================
# CLEANUP CONFIGURATION
# ============================================================================

# Enable automatic cleanup of old containers
# Default: true
AUTO_CLEANUP_ENABLED=true

# Remove containers older than N days
# Default: 7
CLEANUP_DAYS=7

# Keep last N containers (even if older than CLEANUP_DAYS)
# Default: 3
KEEP_LAST_N_CONTAINERS=3

# ============================================================================
# PRE-FLIGHT CHECKS
# ============================================================================

# Minimum Docker version required
# Default: 20.10.0
MIN_DOCKER_VERSION=20.10.0

# Minimum free disk space in MB
# Default: 1000 (1GB)
MIN_DISK_SPACE_MB=1000

# ============================================================================
# ADVANCED CONFIGURATION
# ============================================================================

# Container name prefix
# Default: glm-docker
CONTAINER_NAME_PREFIX=glm-docker

# Enable debug mode by default (true/false)
# Default: false
DEBUG_MODE_DEFAULT=false

# Enable extended thinking mode
# Default: true
EXTENDED_THINKING_ENABLED=true

# Timezone for container
# Default: Europe/Moscow
CONTAINER_TIMEZONE=Europe/Moscow

# ============================================================================
# DEVELOPMENT & TESTING
# ============================================================================

# Dry-run mode by default (true/false)
# Useful for CI/CD testing
# Default: false
DRY_RUN_DEFAULT=false

# Skip pre-flight checks (not recommended)
# Default: false
SKIP_PREFLIGHT_CHECKS=false

# Verbose output (true/false)
# Default: false
VERBOSE=false

# ============================================================================
# NOTES
# ============================================================================
#
# 1. Environment Precedence:
#    Command-line args > .env file > Script defaults
#
# 2. Boolean Values:
#    Use: true/false (lowercase)
#
# 3. Paths:
#    - Use absolute paths for reliability
#    - Tilde (~) is supported for home directory
#    - Variables like $HOME are expanded
#
# 4. Secrets Management:
#    - Never commit .env to git (it's in .gitignore)
#    - Use different .env files for dev/staging/prod
#    - Consider using secrets management tools for production
#
# 5. GitOps Workflow:
#    - Commit .env.example with safe defaults
#    - Each environment has its own .env (not tracked)
#    - CI/CD can generate .env from secrets vault
#
# ============================================================================
