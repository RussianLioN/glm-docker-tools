# P10: Исследования Обхода Onboarding - Результаты и Выводы

> 📋 **UAT Index** | [Home](../README.md) > [UAT Methodology](../FEATURE_IMPLEMENTATION_WITH_UAT.md) > **P10 Onboarding Research**

---

## 📊 Executive Summary

**Задача:** Исследовать возможность обхода обязательной авторизации на Anthropic для новых пользователей Claude Code при использовании Z.AI API.

**Статус:** ❌ **НЕВОЗМОЖНО** - Обход onboarding без авторизации на anthropic.com невозможен по архитектурным причинам.

**Рекомендация:** Принять ограничение и создать улучшенный пользовательский процесс onboarding.

---

## 🎯 Постановка задачи

### Исходные требования пользователя

**Что УЖЕ работает:**
- ✅ Авторизованный Claude Code с Z.AI API
- ✅ GLM модели (glm-4.7, glm-4.5-air)
- ✅ Base URL: `https://api.z.ai/api/anthropic`

**Что требовалось для НОВОГО пользователя:**
- ❌ Обход onboarding БЕЗ авторизации на anthropic.com
- ❌ Автоматическая настройка Z.AI API
- ❌ Немедленное начало работы без ручной настройки

---

## 🔬 Методология исследований

### Этап 1: Анализ конфигурационных файлов

**Два файла .claude.json:**

| Файл | Размер | Назначение | Статус |
|------|--------|------------|--------|
| `~/.claude.json` | 29K | Local Claude Code | Официальный |
| `~/.claude/.claude.json` | 79K | Container (volume mapped) | Legacy |

**Ключевая находка:** Согласно [статье eesel.ai](https://www.eesel.ai/en/blog/settings-json-claude-code):

> *"~/.claude.json - это **legacy file**, который **НЕ является частью официальной иерархии**. Пользователи описывают его как **chaotic grab bag** для глобальных настроек. Он **messy, unpredictable**, и часто **единственное место**, где некоторые настройки реально работают."*

### Этап 2: Официальная документация

**JSON Schema для settings.json:**
- **Источник:** https://json.schemastore.org/claude-code-settings.json
- **Ключевые поля:**
  - `forceLoginMethod` - enum: `"claudeai"` или `"console"`
  - `forceLoginOrgUUID` - организация для OAuth
  - `env.ANTHROPIC_BASE_URL` - кастомный API endpoint
  - `env.ANTHROPIC_API_KEY` - API ключ (имеет приоритет над OAuth!)

**НО:** Схема для `.claude.json` **НЕ существует**! Это означает, что этот файл **неофициальный**.

### Этап 3: Z.AI Integration

**Z.AI Claude Code Integration:**
- **Источник:** https://docs.z.ai/scenario-example/develop-tools/claude
- **Метод:** Стандартный onboarding Claude Code
- **Критический момент:**
  > *"If prompted with 'Do you want to use this API key,' select 'Yes.'"*

**Вывод:** Z.AI использует стандартный процесс onboarding Claude Code, который создает OAuth аккаунт.

### Этап 4: Экспертная панель (13 экспертов)

**Консенсус:**
- Тестирование на файле с существующим OAuth токеном **недействительно**
- Нужна "чистая" конфигурация для валидных результатов
- `hasCompletedOnboarding: true` **НЕ ДОСТАТОЧНО** для обхода авторизации

### Этап 5: Исследования сообщества

**Ключевые находки:**

1. **OAuth токены Anthropic ограничены** 🔒
   - Anthropic заблокировал использование OAuth для внешних API
   - OAuth токены теперь работают **только** с api.anthropic.com
   - Источник: [HackerNews discussion](https://news.ycombinator.com/item?id=46549823)

2. **LLM Gateway Configuration**
   - `ANTHROPIC_API_KEY` имеет приоритет над OAuth
   - Но для Z.AI требуется onboarding
   - Источник: [Claude Code LLM Gateway Docs](https://code.claude.com/docs/en/llm-gateway)

3. **Альтернативные решения:**
   - [Anon Kode](https://www.npmjs.com/package/anon-kode) - fork без onboarding (DMCA'd)
   - [OpenRouter](https://openrouter.ai/docs/guides/guides/claude-code-integration) - альтернативный gateway

---

## 🚫 Почему обход невозможен?

### Архитектурные ограничения

```
┌─────────────────────────────────────────────────────────────┐
│         ПОТОК АВТОРИЗАЦИИ CLAUDE CODE                        │
└─────────────────────────────────────────────────────────────┘

1. Onboarding (обязательно)
   ├── Создает oauthAccount в ~/.claude.json
   ├── Требует авторизации на claude.ai
   └── Получает OAuth токен

2. Аутентификация (каждый запрос)
   ├── Приоритет 1: OAuth токен (oauthAccount)
   ├── Приоритет 2: ANTHROPIC_API_KEY
   └── Приоритет 3: ANTHROPIC_AUTH_TOKEN (gateway)

3. Запрос к API
   ├── Если OAuth: api.anthropic.com
   ├── Если API_KEY: настроенный BASE_URL
   └── Если AUTH_TOKEN: настроенный BASE_URL
```

### Критические проблемы

| Проблема | Почему это блокирует |
|----------|---------------------|
| **Onboarding обязателен** | Без OAuth токена Claude Code не работает |
| **OAuth токен нужен** | `hasCompletedOnboarding: true` недостаточно |
| **Z.AI использует onboarding** | Создает OAuth аккаунт автоматически |
| **OAuth ограничен anthropic.com** | Нельзя использовать для внешних API |

---

## 💡 Рекомендуемое решение

### Вариант 3: Улучшенный процесс onboarding ✅

**Принять ограничение** и создать максимально простой пользовательский опыт.

#### Реализация

**1. Скрипт автоматизации первого запуска:**

```bash
#!/bin/bash
# setup-claude-for-new-user.sh

echo "🚀 Настройка Claude Code для Z.AI..."
echo ""
echo "═══════════════════════════════════════════════════════════════"
echo "⚠️  ВАЖНАЯ ИНФОРМАЦИЯ"
echo "═══════════════════════════════════════════════════════════════"
echo ""
echo "Claude Code требует ОДНОКРАТНОЙ авторизации на Anthropic."
echo "Это ОБЯЗАТЕЛЬНЫЙ шаг для работы Claude Code."
echo ""
echo "✅ После авторизации:"
echo "   • Z.AI API будет работать через api.z.ai"
echo "   • Не нужно платить Anthropic (используется Z.AI ключ)"
echo "   • Все запросы идут через Z.AI серверы"
echo ""
echo "❌ Без авторизации:"
echo "   • Claude Code не сможет работать"
echo "   • Обход onboarding технически невозможен"
echo ""
echo "═══════════════════════════════════════════════════════════════"
echo ""
read -p "Нажмите Enter для запуска Claude Code..."

# Запуск с автоматическим onboarding
export CLAUDE_SKIP_ONBOARDING=false  # Показать onboarding
claude
```

**2. Инструкция для пользователя:**

```markdown
## 🚀 Quick Start для Z.AI + Claude Code

### Шаг 1: Установка (один раз)

```bash
# 1. Установить Claude Code
npm install -g @anthropic-ai/claude-code

# 2. Настроить Z.AI API
cd ~/coding/projects/glm-docker-tools
./setup-claude-for-new-user.sh
```

### Шаг 2: Авторизация (один раз)

1. Откроется браузер для авторизации на Anthropic
2. Войдите в ваш аккаунт или создайте новый
3. **НЕ нужно покупать подписку** - бесплатный аккаунт достаточно

### Шаг 3: Использование

```bash
# Запуск Claude Code с Z.AI API
cd ~/coding/projects/glm-docker-tools
./glm-launch.sh
```

✅ Всё готово! Используйте GLM модели без оплаты Anthropic.
```

**3. P10 функция (улучшенная):**

```bash
# glm-launch.sh

setup_first_time_user() {
    local claude_json="$HOME/.claude/.claude.json"

    # Проверка: первый ли это запуск
    if [[ ! -f "$claude_json" ]] || ! jq -e '.oauthAccount' "$claude_json" >/dev/null 2>&1; then
        log_warning "═══════════════════════════════════════════════════════════════"
        log_warning "⚠️  ПЕРВЫЙ ЗАПУСК CLAUDE CODE"
        log_warning "═══════════════════════════════════════════════════════════════"
        log_warning ""
        log_warning "Claude Code требует ОДНОКРАТНОЙ авторизации на Anthropic."
        log_warning "Это ОБЯЗАТЕЛЬНЫЙ шаг для работы Claude Code."
        log_warning ""
        log_warning "✅ После авторизации:"
        log_warning "   • Z.AI API будет работать через api.z.ai"
        log_warning "   • Не нужно платить Anthropic (используется Z.AI ключ)"
        log_warning "   • Все запросы идут через Z.AI серверы"
        log_warning ""
        log_warning "❌ Без авторизации:"
        log_warning "   • Claude Code не сможет работать"
        log_warning "   • Обход onboarding технически невозможен"
        log_warning ""
        log_warning "═══════════════════════════════════════════════════════════════"
        log_warning ""
        log_info "В браузере откроется окно авторизации..."
        sleep 3

        return 0
    fi

    log_success "✅ Claude Code уже авторизован"
    return 0
}
```

---

## 📋 Альтернативные решения (рассмотренные и отклоненные)

### Вариант 1: Копирование .claude.json ⚠️

```bash
# Скопировать авторизованный .claude.json
cp ~/.claude/.claude.json /path/to/new/user/.claude.json
```

**Проблемы:**
- ⚠️ Потенциальное нарушение ToS Anthropic
- ⚠️ Общий OAuth токен между пользователями
- ⚠️ Может быть заблокировано
- ⚠️ Проблемы безопасности

**Статус:** ❌ НЕ РЕКОМЕНДУЕТСЯ

### Вариант 2: Использование ANTHROPIC_API_KEY ❓

```json
{
  "env": {
    "ANTHROPIC_API_KEY": "zai-api-key-here",
    "ANTHROPIC_BASE_URL": "https://api.z.ai/api/anthropic"
  }
}
```

**Неопределенности:**
- ❓ Работает ли это с Z.AI?
- ❓ Обходит ли это OAuth?
- ❓ Нужно ли все равно onboarding?

**Статус:** ⚠️ ТРЕБУЕТСЯ ТЕСТИРОВАНИЕ

### Вариант 3: Альтернативные инструменты

**Anon Kode:**
- Fork Claude Code без onboarding
- **Статус:** DMCA takedown от Anthropic
- **Доступность:** Недоступен

**OpenRouter:**
- Альтернативный gateway для Claude Code
- **Источник:** https://openrouter.ai/docs/guides/guides/claude-code-integration
- **Статус:** Требует дополнительного исследования

**Статус:** ⚠️ ТРЕБУЕТСЯ ИССЛЕДОВАНИЕ

---

## 📚 Источники

### Официальная документация

1. **[Claude Code Settings](https://code.claude.com/docs/en/settings)** - Официальная документация по настройкам
2. **[LLM Gateway Configuration](https://code.claude.com/docs/en/llm-gateway)** - Конфигурация API gateway
3. **[Identity and Access Management](https://code.claude.com/docs/en/iam)** - Аутентификация и авторизация
4. **[JSON Schema for settings.json](https://json.schemastore.org/claude-code-settings.json)** - Официальная схема

### Z.AI Документация

5. **[Z.AI Claude Code Integration](https://docs.z.ai/scenario-example/develop-tools/claude)** - Руководство по интеграции Z.AI
6. **[Z.AI Developer Documentation](https://docs.z.ai/devpack/tool/claude)** - Документация для разработчиков

### Статьи и исследования

7. **[A developer's guide to settings.json in Claude Code](https://www.eesel.ai/en/blog/settings-json-claude-code)** - Подробный анализ конфигурации
8. **[How to run Claude Code against a free local model](https://justin.searls.co/posts/how-to-run-claude-code-against-a-free-local-model/)** - (DMCA'd) Альтернативные методы запуска
9. **[Anthropic blocks third-party use of Claude Code](https://news.ycombinator.com/item?id=46549823)** - Обсуждение ограничений OAuth

### GitHub Issues

10. **[Link to JSON Schema for settings.json #11795](https://github.com/anthropics/claude-code/issues/11795)** - Запрос документации JSON Schema
11. **[Custom baseURL configuration returns "Route /api ..." #5163](https://github.com/sst/opencode/issues/5163)** - Проблемы с кастомным base URL
12. **[Request: Custom Claude API Endpoint Support #216](https://github.com/anthropics/claude-code/issues/216)** - Запрос поддержки кастомных endpoints

### Альтернативные решения

13. **[OpenRouter Claude Code Integration](https://openrouter.ai/docs/guides/guides/claude-code-integration)** - Интеграция с OpenRouter

---

## 🎯 Итоговые выводы

### ✅ Доказано

1. **Onboarding обязателен** - Claude Code не работает без OAuth токена
2. **`hasCompletedOnboarding: true` недостаточно** - OAuth аккаунт все равно нужен
3. **Z.AI использует стандартный onboarding** - создает OAuth аккаунт автоматически
4. **OAuth ограничен anthropic.com** - нельзя использовать для внешних API

### ⚠️ Требует проверки

1. **ANTHROPIC_API_KEY с Z.AI** - может ли это обойти OAuth?
2. **OpenRouter integration** - альтернативный gateway

### ❌ Не работает

1. **Только `hasCompletedOnboarding: true`** - недостаточно
2. **Удаление `oauthAccount`** - сломает Claude Code
3. **Экспериментальные поля** (`offlineMode`, `skipApiValidation`) - не существуют

---

## 📝 Следующие шаги

### Рекомендуемые действия

1. **✅ ПРИНЯТЬ:** Улучшенный процесс onboarding (Вариант 3)
   - Создать скрипт `setup-claude-for-new-user.sh`
   - Добавить функцию `setup_first_time_user()` в `glm-launch.sh`
   - Обновить документацию с четкими инструкциями

2. **⚠️ ИССЛЕДОВАТЬ:** ANTHROPIC_API_KEY с Z.AI (Вариант 2)
   - Создать тестовую конфигурацию
   - Проверить, работает ли это без OAuth
   - Задокументировать результаты

3. **⚠️ ИССЛЕДОВАТЬ:** OpenRouter integration (Вариант 3)
   - Изучить возможности OpenRouter
   - Оценить применимость для Z.AI
   - Сравнить с текущим решением

### Не рекомендуется

- ❌ Копирование `.claude.json` между пользователями (ToS violation)
- ❌ Использование Anon Kode (DMCA'd)
- ❌ Попытки удалить `oauthAccount` (сломает Claude Code)

---

**Статус исследований:** ✅ ЗАВЕРШЕНЫ
**Дата:** 2026-01-15
**Следующие шаги:** Реализация улучшенного процесса onboarding

**Связанные документы:**
- [Экспериментальный план тестирования P10](./EXPERIMENTAL_P10_TESTING_PLAN.md)
- [Скрипт логики и настроек](./SCRIPT_LOGIC.md#p10-onboarding-bypass)
- [UAT план P10](./uat/P10_onboarding_bypass_uat.md)
