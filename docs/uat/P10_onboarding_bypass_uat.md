# План UAT: P10 - Обход Onboarding

> 📋 **UAT Index** | [Home](../../README.md) > [UAT Methodology](../FEATURE_IMPLEMENTATION_WITH_UAT.md) > **P10 UAT Plan**

## 📊 Метаданные теста

| Поле | Значение |
|------|----------|
| **Feature ID** | P10 - Обход Onboarding |
| **Версия плана** | 1.0 |
| **Дата создания** | 2026-01-14 |
| **Автор плана** | Claude Code (13-expert panel consensus) |
| **Подход UAT** | v2.0 Гибридный (70-80% AI-auto, 20-30% User-practical) |
| **Оценка времени** | ~5 минут пользовательского тестирования |

---

## 🎯 Краткое описание функции

**Функция:** Автоматический обход экрана onboarding при запуске Docker контейнера

**Проблема:** Claude Code требует обязательной авторизации на сайте Anthropic при каждом запуске контейнера

**Решение:** Скрипт автоматически устанавливает флаг `hasCompletedOnboarding: true` в `~/.claude/.claude.json` при запуске

**Ключевые характеристики:**
- Защитная реализация с бэкапом перед модификацией
- Атомарные операции через jq
- Идемпотентность (безопасно запускать многократно)
- Управление через переменную окружения `CLAUDE_SKIP_ONBOARDING`

---

## ✅ Критерии приемки (Acceptance Criteria)

### Функциональные требования
- [ ] `hasCompletedOnboarding: true` устанавливается в `~/.claude/.claude.json`
- [ ] Экран onboarding не появляется при запуске контейнера
- [ ] Бэкап создается перед модификацией файла
- [ ] Управление через переменную окружения работает
- [ ] Идемпотентность (повторные запуски безопасны)
- [ ] Graceful degradation (продолжение работы если jq недоступен)

### Нефункциональные требования
- [ ] Защитная реализация (бэкап, валидация, откат при ошибке)
- [ ] Интеграция с P6/P7/P8/P9
- [ ] Четкие сообщения пользователю
- [ ] Логирование всех операций
- [ ] Документация обновлена

---

## 🧪 План тестирования UAT v2.0

### Phase 1: AI-Automated Checks (70-80%)

Эти проверки выполняются AI автоматически без участия пользователя.

| ID | Проверка | Команда | Ожидаемый результат |
|----|----------|---------|---------------------|
| **AI-1** | Функция `set_onboarding_flag()` существует | `grep -A 50 "set_onboarding_flag()" glm-launch.sh | head -60` | Найдена функция с реализацией |
| **AI-2** | Проверка зависимости jq в `check_dependencies()` | `grep -A 5 "CLAUDE_SKIP_ONBOARDING" glm-launch.sh | grep "command -v jq"` | Найдена проверка jq |
| **AI-3** | Создание бэкапа перед модификацией | `grep "cp.*claude_json.*backup" glm-launch.sh` | Найдена команда бэкапа |
| **AI-4** | Атомарная запись через временный файл | `grep "mv.*tmp_file.*claude_json" glm-launch.sh` | Найдена атомарная операция mv |
| **AI-5** | Валидация JSON после записи | `grep "jq empty.*tmp_file" glm-launch.sh` | Найдена валидация JSON |
| **AI-6** | Интеграция с `run_claude()` | `grep -A 3 "CLAUDE_SKIP_ONBOARDING" glm-launch.sh | grep "set_onboarding_flag"` | Найден вызов функции |
| **AI-7** | Переменная в `secrets/.env.example` | `grep "CLAUDE_SKIP_ONBOARDING" secrets/.env.example` | Найдена переменная с документацией |
| **AI-8** | Документация P10 в SCRIPT_LOGIC.md | `grep "P10.*Onboarding" docs/SCRIPT_LOGIC.md` | Найдена секция P10 |

### Phase 2: User-Practical Tests (20-30%)

Эти тесты выполняются пользователем для проверки реального сценария использования.

**Важно:** Согласно методологии UAT v2.0, пользовательские тесты максимально упрощены - только запуск скрипта. Все проверки выполняет AI автоматически.

#### Test U-1: Проверка работы обхода onboarding (Практический тест)

**Цель:** Убедиться, что скрипт автоматически устанавливает флаг onboarding

**Предусловия:**
- Docker установлен и запущен
- `glm-launch.sh` доступен
- `jq` установлен

**Действие пользователя (30 секунд):**

```bash
# Просто запустить скрипт
./glm-launch.sh
```

**Что делает скрипт автоматически (AI-verified):**
1. ✅ Проверяет наличие `jq`
2. ✅ Читает `CLAUDE_SKIP_ONBOARDING=true` из `secrets/.env`
3. ✅ Создает бэкап `~/.claude.json.bak.$$`
4. ✅ Устанавливает `hasCompletedOnboarding: true` в `~/.claude.json`
5. ✅ Проверяет успешность установки

**Ожидаемый результат:**
- ✅ В выводе видно: "✅ Onboarding bypass enabled"
- ✅ Экран onboarding не появляется при следующем запуске

**Время выполнения:** ~30 секунд

**AI Verification (автоматически после теста):**
```bash
# AI проверяет, что флаг установлен
jq '.hasCompletedOnboarding' ~/.claude.json
# Ожидается: true
```

---

## 🔧 Восстановление после тестирования

Для восстановления оригинальной конфигурации используйте бэкапы, созданные автоматически:

```bash
# Восстановить из бэкапа (если нужно)
ls -la ~/.claude-p10-backup/
cp ~/.claude-p10-backup/home-claude.json.backup-* ~/.claude.json

echo "✅ Конфигурация восстановлена"
```

---

## 📋 Чеклист выполнения UAT

### AI-Automated Checks (выполняются AI)
- [ ] AI-1: Функция `set_onboarding_flag()` существует
- [ ] AI-2: Проверка jq зависимости
- [ ] AI-3: Создание бэкапа
- [ ] AI-4: Атомарная запись
- [ ] AI-5: Валидация JSON
- [ ] AI-6: Интеграция с run_claude()
- [ ] AI-7: Переменная в secrets/.env.example
- [ ] AI-8: Документация P10 в SCRIPT_LOGIC.md
- [ ] AI-9: ✅ Исправлен путь к ~/.claude.json (правильный файл)
- [ ] AI-10: ✅ Добавлены источники в комментариях функции

### User-Practical Tests (выполняются пользователем)
- [ ] U-1: Запуск `./glm-launch.sh` (30 секунд)
- [ ] Восстановление из бэкапа (если нужно)

---

## 🐛 Troubleshooting

### Проблема: "jq: command not found"

**Решение:**
```bash
# Установить jq (macOS)
brew install jq

# Установить jq (Linux)
sudo apt-get install jq  # Debian/Ubuntu
sudo yum install jq      # CentOS/RHEL
```

### Проблема: "Permission denied" при записи в ~/.claude/.claude.json

**Решение:**
```bash
# Проверить права доступа
ls -la ~/.claude/.claude.json

# Если нужно, исправить права
chmod 644 ~/.claude/.claude.json
```

### Проблема: Флаг не устанавливается

**Диагностика:**
```bash
# Проверить, что переменная установлена
echo $CLAUDE_SKIP_ONBOARDING
# Ожидается: true

# Проверить содержимое secrets/.env
cat secrets/.env | grep CLAUDE_SKIP_ONBOARDING

# Проверить вывод скрипта
./glm-launch.sh --dry-run 2>&1 | grep -i onboarding
```

---

## 📊 Результаты UAT

### Дата выполнения: _______________

### AI-Automated Checks
| Проверка | Статус | Примечания |
|----------|--------|------------|
| AI-1 | ⏳ Pending | |
| AI-2 | ⏳ Pending | |
| AI-3 | ⏳ Pending | |
| AI-4 | ⏳ Pending | |
| AI-5 | ⏳ Pending | |
| AI-6 | ⏳ Pending | |
| AI-7 | ⏳ Pending | |
| AI-8 | ⏳ Pending | |

### User-Practical Tests
| Тест | Статус | Примечания |
|------|--------|------------|
| U-1 | ⏳ Pending | |
| U-2 | ⏳ Pending | |

### Итоговый вердикт
- [ ] **UAT PASSED** - Все проверки пройдены успешно
- [ ] **UAT FAILED** - Обнаружены критические проблемы
- [ ] **UAT CONDITIONAL** - Пройдено с замечаниями

**Подпись тестировщика:** _________________

**Дата:** _______________

---

## 🔗 Связанные документы

- **[Методология UAT v2.0](../FEATURE_IMPLEMENTATION_WITH_UAT.md)** - Полная методология гибридного тестирования
- **[План реализации P10](../../../.claude/plans/distributed-hugging-token.md)** - Детальный план реализации
- **[Руководство по секретам](../SECRETS_MANAGEMENT.md)** - Управление секретами и переменными окружения
- **[Логика скриптов](../SCRIPT_LOGIC.md)** - Документация логики работы скриптов

---

**Версия:** 1.0
**Последнее обновление:** 2026-01-14
**Статус:** ✅ ГОТОВ К ВЫПОЛНЕНИЮ
