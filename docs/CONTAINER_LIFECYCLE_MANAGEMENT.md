# Container Lifecycle Management Guide

> 🏠 [Home](../README.md) > **📚 Documentation** > **Container Lifecycle**

## 🔄 Управление жизненным циклом контейнеров Claude Code

Это руководство описывает управление жизненным циклом Docker контейнеров через скрипты запуска с поддержкой различных режимов работы.

---

## 📋 Обзор режимов работы

Скрипт `glm-launch.sh` поддерживает три режима управления жизненным циклом контейнеров с **оптимизированным использованием ресурсов**:

### 🗑️ **Стандартный режим (Auto-delete)**
```bash
./glm-launch.sh
```
- **Флаг Docker**: `--rm`
- **Поведение**: Контейнер автоматически удаляется при выходе
- **Имя**: Уникальное `glm-docker-{timestamp}`
- **Состояние**: Удален (~0MB память)
- **Использование**: Ежедневная работа, временные задачи

### 🔧 **Debug режим**
```bash
./glm-launch.sh --debug
```
- **Флаг Docker**: Без `--rm`
- **Поведение**: Контейнер **остается ЗАПУЩЕННЫМ** после выхода из Claude
- **Имя**: Уникальное `glm-docker-debug-{timestamp}`
- **Состояние**: **RUNNING** (~50-100MB память)
- **Использование**: Отладка, исследование проблем, частый доступ
- **Shell доступ**: Немедленный через `docker exec -it <container> /bin/bash`

### 💾 **No-del режим (Resource-Efficient)**
```bash
./glm-launch.sh --no-del
```
- **Флаг Docker**: Без `--rm`
- **Поведение**: Контейнер **ОСТАНАВЛИВАЕТСЯ** после выхода из Claude
- **Имя**: Уникальное `glm-docker-nodebug-{timestamp}`
- **Состояние**: **STOPPED** (~0MB память)
- **Использование**: Долгосрочные задачи, ресурсоэффективность
- **Shell доступ**: Через `./scripts/shell-access.sh <container>`

**⭐ Архитектурное улучшение v1.2.0:**
- ✅ **No-del режим** теперь останавливает контейнеры (~0MB когда не используется)
- ✅ **Debug режим** оставляет контейнеры запущенными (мгновенный shell доступ)
- ✅ **Утилита shell-access.sh** для удобного доступа к остановленным контейнерам

---

## 🚀 Практическое использование

### Повседневная работа
```bash
# Стандартный запуск с автоудалением
./glm-launch.sh

# С конкретной командой
./glm-launch.sh --help
./glm-launch.sh /status
```

### Отладка и разработка (Debug режим)
```bash
# Debug режим для отладки (контейнер остается ЗАПУЩЕННЫМ)
./glm-launch.sh --debug

# После выхода из Claude - автоматически в shell контейнера
# Контейнер: glm-docker-debug-{timestamp} (RUNNING)

# В контейнере можно:
ls -la /root/.claude/
cat /root/.claude/settings.json
claude --version

# Для повторного запуска Claude:
docker exec -it glm-docker-debug-{timestamp} claude

# Для выхода из shell:
exit

# Остановить debug контейнер когда не нужен:
docker stop glm-docker-debug-{timestamp}
```

### Долгосрочные задачи (No-del режим)
```bash
# No-del режим для долгосрочных задач (контейнер ОСТАНАВЛИВАЕТСЯ)
./glm-launch.sh --no-del

# После выхода из Claude - контейнер ОСТАНОВЛЕН (~0MB память)
# Контейнер: glm-docker-nodebug-{timestamp} (STOPPED)

# Повторный запуск Claude:
docker start -ai glm-docker-nodebug-{timestamp}

# Удобный shell доступ с автоматическим управлением:
./scripts/shell-access.sh glm-docker-nodebug-{timestamp}
# Автоматически: запустит → откроет shell → остановит при выходе

# Ручной shell доступ (3 команды):
docker start glm-docker-nodebug-{timestamp}
docker exec -it glm-docker-nodebug-{timestamp} /bin/bash
# ... работа в shell ...
docker stop glm-docker-nodebug-{timestamp}

# Удалить когда не нужен:
docker rm -f glm-docker-nodebug-{timestamp}
```

---

## 🛠️ Управление контейнерами

### Просмотр контейнеров
```bash
# Активные контейнеры
docker ps

# Все контейнеры (включая остановленные)
docker ps -a

# Фильтр контейнеров проекта
docker ps -a --filter "name=glm-docker"
```

### Подключение к контейнерам
```bash
# === Debug режим (RUNNING контейнеры) ===

# Подключение к Claude в запущенном контейнере
docker exec -it glm-docker-debug-{timestamp} claude

# Подключение к shell запущенного контейнера
docker exec -it glm-docker-debug-{timestamp} /bin/bash

# Остановить debug контейнер
docker stop glm-docker-debug-{timestamp}

# === No-del режим (STOPPED контейнеры) ===

# Повторный запуск Claude
docker start -ai glm-docker-nodebug-{timestamp}

# Удобный shell доступ (одна команда)
./scripts/shell-access.sh glm-docker-nodebug-{timestamp}

# Ручной shell доступ (три команды)
docker start glm-docker-nodebug-{timestamp}
docker exec -it glm-docker-nodebug-{timestamp} /bin/bash
docker stop glm-docker-nodebug-{timestamp}
```

### Утилита shell-access.sh ⭐ NEW

**Удобный shell доступ к остановленным контейнерам**

```bash
# Использование
./scripts/shell-access.sh <container-name>

# Примеры
./scripts/shell-access.sh glm-docker-nodebug-1234567890
./scripts/shell-access.sh glm-docker-debug-1234567890

# Что происходит автоматически:
# 1. Проверка состояния контейнера
# 2. Запуск если остановлен
# 3. Открытие /bin/bash shell
# 4. Остановка при выходе из shell
```

**Преимущества:**
- ✅ Одна команда вместо трех (docker start + exec + stop)
- ✅ Автоматическая проверка и управление состоянием
- ✅ Работает с остановленными и запущенными контейнерами
- ✅ Ресурсоэффективно для no-del режима

### Очистка контейнеров
```bash
# Остановить все контейнеры проекта
docker ps -q --filter "name=glm-docker" | xargs docker stop

# Удалить все остановленные контейнеры проекта
docker ps -aq --filter "name=glm-docker" | xargs docker rm

# Полная очистка (остановить и удалить)
docker ps -aq --filter "name=glm-docker" | xargs -r docker stop | xargs -r docker rm
```

---

## 📊 Сравнение режимов

| Характеристика | Auto-delete | Debug | No-del |
|----------------|-------------|-------|---------|
| **Автоудаление** | ✅ Да | ❌ Нет | ❌ Нет |
| **Состояние после Claude** | 🗑️ Удален | 💾 **RUNNING** | ⏸️ **STOPPED** |
| **Использование памяти** | ~0MB | ~50-100MB | ~0MB |
| **Shell доступ** | ❌ Нет | ✅ Прямой `docker exec` | ⚠️ Через `shell-access.sh` |
| **Имя контейнера** | Уникальное | Уникальное | Уникальное |
| **Сохранение состояния** | ❌ Нет | ✅ Да | ✅ Да |
| **Отладка** | ❌ Нет | ✅ Да | ⚠️ Ограничено |
| **Повседневное использование** | ✅ Идеально | ❌ Избыточно | ⚠️ Только длительные задачи |
| **Ресурсоэффективность** | ✅ Отлично | ⚠️ Потребляет ресурсы | ✅ Отлично |

---

## 🔧 Примеры использования

### Пример 1: Ежедневная работа
```bash
# Запуск с автоудалением
./glm-launch.sh

# Работа в Claude...
# Выход - контейнер автоматически удаляется
```

### Пример 2: Отладка проблемы (Debug режим)
```bash
# Запуск в debug режиме (контейнер остается ЗАПУЩЕННЫМ)
./glm-launch.sh --debug

# Работа в Claude, воспроизведение проблемы
# Выход из Claude -> автоматический переход в shell контейнера

# В shell контейнера можно исследовать:
ls -la /root/.claude/
cat /root/.claude/logs/
claude --version

# Повторный запуск Claude без выхода:
docker exec -it glm-docker-debug-{timestamp} claude

# Выход из shell (контейнер продолжает работать)
exit

# Когда debug больше не нужен - остановить контейнер:
docker stop glm-docker-debug-{timestamp}
```

### Пример 3: Долгосрочный проект (No-del режим)
```bash
# Запуск в no-del режиме (контейнер ОСТАНАВЛИВАЕТСЯ после Claude)
./glm-launch.sh --no-del

# Работа в Claude над проектом...
# Выход из Claude -> контейнер автоматически ОСТАНАВЛИВАЕТСЯ

# Память не занята (~0MB) - контейнер остановлен

# Позже - повторный запуск Claude:
docker start -ai glm-docker-nodebug-{timestamp}

# Или shell доступ для операций:
./scripts/shell-access.sh glm-docker-nodebug-{timestamp}
# (Автоматически запустит, откроет shell, остановит при выходе)

# Когда проект завершен - удалить контейнер:
docker rm -f glm-docker-nodebug-{timestamp}
```

### Пример 4: Множественные сессии
```bash
# Запуск нескольких контейнеров для разных задач
./glm-launch.sh --no-del  # Контейнер 1
./glm-launch.sh --no-del  # Контейнер 2
./glm-launch.sh --no-del  # Контейнер 3

# Просмотр всех сессий
docker ps -a --filter "name=glm-docker"

# Подключение к нужной сессии
docker exec -it glm-docker-1234567890 claude
```

---

## 🧪 Тестирование функциональности

### Запуск тестов
```bash
# Полный тест всех режимов
./scripts/test-container-lifecycle.sh

# Тест с реальным контейнером
./scripts/test-container-lifecycle.sh --test-real

# Только очистка
./scripts/test-container-lifecycle.sh --cleanup
```

### Валидация режимов
```bash
# Проверка auto-delete режима
./glm-launch.sh --dry-run | grep -q "\-\-rm"

# Проверка debug режима
./glm-launch.sh --debug --dry-run | grep -q "claude-debug"

# Проверка no-del режима
./glm-launch.sh --no-del --dry-run | grep -v "\-\-rm"

# Проверка валидации конфликтов
./glm-launch.sh --debug --no-del 2>&1 | grep -q "Нельзя использовать"
```

---

## ⚠️ Рекомендации

### Когда использовать Auto-delete (стандартный режим)
- ✅ Ежедневная работа и обычные задачи
- ✅ Временные эксперименты
- ✅ Когда не нужно сохранять состояние
- ✅ Автоматическая очистка ресурсов

### Когда использовать Debug режим
- ✅ Отладка проблем с конфигурацией
- ✅ Исследование внутренней работы контейнера
- ✅ Тестирование новых возможностей
- ✅ Доступ к файловой системе контейнера

### Когда использовать No-del режим
- ✅ Долгосрочные проекты с сохранением состояния
- ✅ Необходимость сохранения данных между сессиями
- ✅ Множественные рабочие окружения
- ✅ Прерванные задачи, которые нужно продолжить

---

## 🔍 Поиск и устранение проблем

### Контейнер не удаляется
```bash
# Проверка зависших процессов
docker ps -a --filter "name=glm-docker"

# Принудительное удаление
docker rm -f glm-docker-timestamp

# Очистка всех контейнеров проекта
./scripts/test-container-lifecycle.sh --cleanup
```

### Проблемы с debug режимом
```bash
# Проверка существующего debug контейнера
docker ps -a --filter "name=claude-debug"

# Удаление старого debug контейнера
docker rm -f claude-debug

# Новый запуск debug режима
./glm-launch.sh --debug
```

### Конфликт имен
```bash
# Просмотр всех контейнеров проекта
docker ps -a --filter "name=glm-docker"

# Удаление конфликтующих контейнеров
docker ps -aq --filter "name=glm-docker" | xargs -r docker rm -f
```

---

## 📚 Связанная документация

- **[Запуск скриптов](../README.md#docker-setup)** - Основная настройка Docker
- **[Тестирование](./NANO_EDITOR_SETUP.md)** - Тестирование интеграции nano
- **[Управление контейнерами](../scripts/launch-multiple.sh)** - Множественные контейнеры
- **[Troubleshooting](../SECURITY.md#troubleshooting)** - Общие решения проблем

---

## ✅ Контрольный чеклист

### Перед запуском
- [ ] Docker запущен и доступен
- [ ] Образ glm-docker-tools:latest загружен
- [ ] Директория ~/.claude существует
- [ ] Настройки API корректны

### При использовании Auto-delete
- [ ] Понимаю, что контейнер будет удален
- [ ] Сохраняю важные данные вне контейнера
- [ ] Проверяю логи перед выходом

### При использовании Debug режима
- [ ] Контейнер claude-debug будет заменен
- [ ] Будет предоставлен shell доступ
- [ ] Можно вручную удалить контейнер после

### При использовании No-del режима
- [ ] Контейнеры будут накапливаться
- [ ] Планую регулярную очистку
- [ ] Сохраняю имена контейнеров для доступа

---

**Статус**: ✅ **Production Ready** - Все режимы протестированы и документированы

**Версия**: 1.1.0
**Совместимость**: Claude Code Docker Integration
**Последнее обновление**: 2025-12-22