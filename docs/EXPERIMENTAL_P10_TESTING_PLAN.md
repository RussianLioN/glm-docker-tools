# План Экспериментального Тестирования P10

> 📋 **UAT Index** | [Home](../README.md) > [UAT Methodology](../FEATURE_IMPLEMENTATION_WITH_UAT.md) > **P10 Experimental Testing**

## 🎯 Цель Тестирования

**Проблема:** Claude Code пытается авторизоваться на api.anthropic.com, даже при:
- ✅ `hasCompletedOnboarding: true`
- ✅ GLM API ключ настроен
- ✅ `ANTHROPIC_BASE_URL` указан на api.z.ai

**Гипотеза:** Необходимы дополнительные поля или действия для полного отключения anthropic.com

---

## 📊 Матрица Тестов

| # | Вариант | Конфигурация | Ожидаемый результат | Статус |
|---|---------|--------------|---------------------|--------|
| 1 | **Базовый** | Только `hasCompletedOnboarding: true` | anthropic.com | ⏳ Pending |
| 2 | **Без OAuth** | Базовый + удалить `oauthAccount` | anthropic.com | ⏳ Pending |
| 3 | **Trust Flags** | Базовый + `hasTrustDialogHooksAccepted: true` | anthropic.com | ⏳ Pending |
| 4 | **Экспериментальный** | Базовый + экспериментальные поля | ? | ⏳ Pending |
| 5 | **Комбинированный** | Все вместе | ? | ⏳ Pending |
| 6 | **Контейнерный** | Настроить `~/.claude/.claude.json` | ? | ⏳ Pending |

---

## 🔬 Детальные Планы Тестов

### Тест 1: Базовая Конфигурация

**Цель:** Проверить что происходит с базовой конфигурацией

**Конфигурация:**
```json
{
  "hasCompletedOnboarding": true
}
```

**Шаги:**
1. Сохранить текущее состояние
2. Запустить `./glm-launch.sh` с `CLAUDE_SKIP_ONBOARDING=false` (отключить наш скрипт)
3. Проверить - пытается ли подключиться к anthropic.com

**Ожидание:** ❌ Попытка подключения к anthropic.com

---

### Тест 2: Без OAuth

**Цель:** Проверить влияние `oauthAccount`

**Конфигурация:**
```json
{
  "hasCompletedOnboarding": true
}
```
**Условие:** Удалить поле `oauthAccount` полностью

**Шаги:**
1. Сделать бэкап
2. Удалить `oauthAccount` из `~/.claude.json`
3. Запустить контейнер
4. Проверить подключения

**Ожидание:** ❌ Всё ещё попытка (но проверим)

---

### Тест 3: Trust Flags

**Цель:** Проверить `hasTrustDialogHooksAccepted`

**Конфигурация:**
```json
{
  "hasCompletedOnboarding": true,
  "hasTrustDialogHooksAccepted": true
}
```

**Шаги:**
1. Добавить `hasTrustDialogHooksAccepted: true`
2. Запустить контейнер
3. Проверить подключения

**Ожидание:** ❌ Возможно всё ещё попытка

---

### Тест 4: Экспериментальные Поля

**Цель:** Протестировать нестандартные поля из сообщества

**Конфигурация:**
```json
{
  "hasCompletedOnboarding": true,
  "hasTrustDialogHooksAccepted": true,
  "offlineMode": true,
  "skipApiValidation": true,
  "disableOAuth": true
}
```

**Шаги:**
1. Добавить экспериментальные поля
2. Запустить контейнер
3. Проверить - работают ли эти поля

**Ожидание:** ⚠️ Неизвестно (поля могут не существовать)

---

### Тест 5: Комбинированный Подход

**Цель:** Максимум настроек

**Конфигурация:**
```json
{
  "hasCompletedOnboarding": true,
  "hasTrustDialogHooksAccepted": true,
  "offlineMode": true,
  "skipApiValidation": true,
  "disableOAuth": true,
  "bypassAnthropicCheck": true,
  "skipNetworkCheck": true
}
```

**Environment Variables:**
```bash
export ANTHROPIC_OFFLINE=true
export CLAUDE_SKIP_VALIDATION=true
```

**Шаги:**
1. Применить все настройки
2. Установить env vars
3. Запустить контейнер
4. Проверить

**Ожидание:** ⚠️ Неизвестно

---

### Тест 6: Контейнерный Файл

**Цель:** Настроить `~/.claude/.claude.json` (который мапится в контейнер)

**Конфигурация `~/.claude/.claude.json`:**
```json
{
  "hasCompletedOnboarding": true,
  "hasTrustDialogHooksAccepted": true
}
```

**Шаги:**
1. Настроить `~/.claude/.claude.json`
2. Убедиться что файл мапится в контейнер
3. Запустить контейнер
4. Проверить

**Ожидание:** ⚠️ Возможно это РЕШЕНИЕ!

---

## 🧪 Процедура Тестирования

### Для каждого теста:

```bash
#!/bin/bash
# 1. Create backup
BACKUP_DIR="$HOME/.claude-p10-test-$(date +%Y%m%d_%H%M%S)"
mkdir -p "$BACKUP_DIR"
cp ~/.claude.json "$BACKUP_DIR/"
echo "✅ Backup: $BACKUP_DIR"

# 2. Apply configuration
# [configuration specific commands]

# 3. Run container
export CLAUDE_SKIP_ONBOARDING=false  # Disable our script
./glm-launch.sh &
CONTAINER_PID=$!

# 4. Monitor network connections
timeout 30 tcpdump -i any host api.anthropic.com 2>/dev/null &
TCPDUMP_PID=$!

# 5. Check result
wait $CONTAINER_PID
EXIT_CODE=$?

if kill -0 $TCPDUMP_PID 2>/dev/null; then
    kill $TCPDUMP_PID
fi

echo "Exit code: $EXIT_CODE"
```

---

## 📋 Результаты Тестов

| # | Дата | Конфигурация | anthropic.com | Успех? | Заметки |
|---|------|--------------|---------------|---------|---------|
| 1 | | | | | |
| 2 | | | | | |
| 3 | | | | | |
| 4 | | | | | |
| 5 | | | | | |
| 6 | | | | | |

---

## 🎯 Критерии Успеха

**Тест считается УСПЕШНЫМ если:**
- ✅ Контейнер запускается БЕЗ попытки подключения к api.anthropic.com
- ✅ Claude Code работает с GLM API (api.z.ai)
- ✅ Можно выполнять команды (Read, Edit, Bash)

---

## 🔄 Rollback После Тестов

```bash
# Восстановить из бэкапа
cp ~/.claude-p10-test-*/home-claude.json.backup-* ~/.claude.json

# Или из основного бэкапа
cp ~/.claude-p10-backup/home-claude.json.backup-* ~/.claude.json
```

---

**Статус:** 🔄 ГОТОВ К ТЕСТИРОВАНИЮ
**Дата:** 2026-01-14
**Следующие шаги:** Выполнить тесты последовательно, записать результаты
