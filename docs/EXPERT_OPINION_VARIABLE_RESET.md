# Экспертное мнение: Сброс переменных окружения перед запуском

## 🚨 Критическая проблема: Конфликт переменных окружения

### **1. Senior Docker Engineer** (Ключевое мнение)

**Анализ:**
> "Запуск нового решения с переменными от старого скрипта - это прямой путь к катастрофе. Переменные CLAUDE_STATE_DIR, CLAUDE_CONFIG и другие будут конфликтовать с нашей логикой."

**Абсолютное требование:**
```bash
# ОБЯЗАТЕЛЬНО сбросить все старые переменные
unset CLAUDE_STATE_DIR
unset CLAUDE_CONFIG
unset CLAUDE_CONFIG_FILE
unset STATE_DIR
unset GLM_STATE_DIR
```

**Техническое обоснование:**
- Старые переменные могут переопределить наши настройки
- Конфликт volume mapping приведет к неправильным путям
- Риск повреждения данных или потери функциональности

---

### **2. Unix Script Expert** (Мастер Bash/Zsh)

**Анализ:**
> "Shell сессия загрязнена переменными от предыдущего запуска. Даже если мы устанавливаем свои переменные, они могут быть переопределены в дочерних процессах или functions."

**Решение:**
```bash
#!/bin/bash
# Полный сброс окружения
unset ${!CLAUDE*}
unset ${!STATE*}

# Или принудительный экспорт
export CLAUDE_STATE_DIR="$HOME/.claude"
export CLAUDE_CONFIG_DIR="/root/.claude"
env | grep CLAUDE  # Проверить после сброса
```

---

### **3. DevOps Engineer** (Automation & Deployment)

**Анализ:**
> "В production окружении это критическая уязвимость. Переменные окружения должны быть полностью контролируемы для предсказуемого поведения системы."

**Минимальные требования:**
```bash
# Создание чистого окружения
env -i bash -c '
    CLAUDE_STATE_DIR="$HOME/.claude"
    ./claude-launch.sh
'
```

---

### **4. CI/CD Architect** (Pipeline Design)

**Анализ:**
> "В CI/CD pipeline мы всегда начинаем с чистого окружения. Аналогично для локальной разработки - нужна изоляция."

**Рекомендация:**
```bash
# Создание изолированного subshell
(
    # Сброс всех переменных
    unset ${!CLAUDE*}

    # Установка наших переменных
    export CLAUDE_STATE_DIR="$HOME/.claude"
    export CLAUDE_CONFIG_DIR="/root/.claude"

    # Запуск
    ./claude-launch.sh "$@"
)
```

---

### **5. GitOps Specialist** (GitOps 2.0 Architecture)

**Анализ:**
> "GitOps принцип требует декларативного подхода. Переменные должны быть определены в коде, а не унаследованы из внешних источников."

**Стратегия:**
```yaml
# docker-compose.yml
services:
  claude:
    environment:
      - CLAUDE_STATE_DIR=/root/.claude
      - CLAUDE_CONFIG_DIR=/root/.claude
    env_file:
      - .claude.env
```

---

### **6. Infrastructure as Code Expert** (IaC Best Practices)

**Анализ:**
> "Imperative скрипты с inherited environment variables нарушают принципы IaC. Configuration should be explicit and versioned."

**Нарушение IaC принципов:**
- ❌ Унаследованные переменные
- ❌ Неявная конфигурация
- ❌ Зависимость от внешнего состояния

**Правильный подход:**
- ✅ Явное определение всех переменных
- ✅ Изоляция конфигурации
- ✅ Версионирование настроек

---

### **7. Backup & Disaster Recovery Specialist** (Data Safety)

**Анализ:**
> "Запуск с неконтролируемыми переменными может повредить данные. Особенно опасны переменные путей - они могут привести к записи не в те файлы."

**Риски без сброса:**
- Потеря данных при записи в неправильные места
- Коррупция истории чатов
- Невозможность восстановления состояния

---

### **8. SRE** (Site Reliability Engineer) (Production Reliability)

**Анализ:**
> "Observability страдает при конфликте переменных. Невозможно определить какие настройки реально применяются в runtime."

**Monitoring recommendation:**
```bash
# Включить отладку для проверки переменных
set -x
export CLAUDE_DEBUG=1
./claude-launch.sh

# Проверить фактические переменные в контейнере
docker exec claude-code env | grep CLAUDE
```

---

### **9. Эксперт по AI IDE** (Claude Code & Others)

**Анализ:**
> "Claude Code чувствителен к CLAUDE_CONFIG_DIR. Неправильное значение может привести к потере MCP конфигурации, расширений и истории."

**Критические переменные:**
- `CLAUDE_CONFIG_DIR` - определяет где искать конфигурацию
- `CLAUDE_STATE_DIR` - влияет на монтирование томов
- `CLAUDE_CONFIG_FILE` - переопределяет путь к файлу конфигурации

---

### **10. Промпт инженер высшего уровня**

**Анализ:**
> "Проблема в том, что состояние системы не детерминировано. Без полного понимания всех переменных, которые могут повлиять на запуск, результат непредсказуем."

**Принцип безопасности:**
1. **Начинай с чистого листа** - всегда сбрасывай переменные
2. **Определяй все явно** - не полагайся на наследование
3. **Проверяй результат** - валидируй что применились правильные значения

---

## 🔥 ЕДИНОГЛАСНОЕ МНЕНИЕ ЭКСПЕРТОВ

### **АБСОЛЮТНО ОБЯЗАТЕЛЬНО:**
1. **Нельзя запускать без полного сброса переменных**
2. **Нужно принудительно очищать все CLAUDE* и STATE* переменные**
3. **Нельзя запускать в текущей shell сессии**

### **РЕКОМЕНДОВАННЫЙ ПОДХОД:**

#### **Вариант 1: Новая shell сессия (RECOMMENDED)**
```bash
# Полностью новая сессия
exec zsh

# Теперь запуск безопасен
./claude-launch.sh /resume
```

#### **Вариант 2: Изолированный subshell**
```bash
# Создание изолированной среды
(
    # Полный сброс переменных
    unset ${!CLAUDE*} ${!STATE*}

    # Запуск нашего решения
    ./claude-launch.sh "$@"
)
```

#### **Вариант 3: Принудительный сброс (WORKS)**
```bash
# Полный сброс всех переменных
unset ${!CLAUDE*} ${!STATE*}

# Запуск с проверкой
CLAUDE_STATE_DIR="$HOME/.claude" \
CLAUDE_CONFIG_DIR="/root/.claude" \
./claude-launch.sh "$@"
```

### ⚠️ ЧТО БУДЕТ БЕЗ СБРОСА ПЕРЕМЕННЫХ:
- ❌ Неправильные volume paths
- ❌ Конфликт конфигурации
- ❌ Потеря функциональности
- ❌ Возможная порча данных

### ✅ ПОСЛЕ ПРАВИЛЬНОГО СБРОСА:
- ✅ Предсказуемое поведение
- ✅ Правильное volume mapping
- ✅ Унифицированная история чатов
- ✅ Безопасная и надежная работа

---

## 🎯 **ВЕРДИКТ: ЗАПРЕЩАТЬ ЗАПУСК БЕЗ СБРОСА ПЕРЕМЕННЫХ!**