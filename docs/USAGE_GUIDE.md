# Инструкция по запуску Claude с исправленной конфигурацией

## 🎯 Краткая инструкция

### 1. Быстрый запуск (рекомендуется)

```bash
# Шаг 1: Сбросить старые переменные
unset CLAUDE_STATE_DIR

# Шаг 2: Запустить с правильной конфигурацией
CLAUDE_STATE_DIR="$HOME/.claude" ./ai-assistant.zsh

# Шаг 3: Внутри контейнера проверить историю
claude /resume  # Должна показать полную историю
```

### 2. Запуск в новой shell сессии

```bash
# Начать новую сессию (все переменные пересоздадутся)
exec zsh

# Запустить Claude
./ai-assistant.zsh
```

---

## 📋 Полная пошаговая инструкция

### Предварительные требования
- Docker должен быть запущен
- Должен быть доступен образ `claude-code-tools:latest`

### Шаг 0: Проверка текущего состояния
```bash
# Проверить backup файлы
ls -la ~/.claude.backup.*
ls -la ~/.claude.json.backup.*

# Проверить файлы проекта
ls -la ai-assistant.zsh*
```

### Шаг 1: Подготовка окружения
```bash
# Очистить старые переменные
unset CLAUDE_STATE_DIR
unset CLAUDE_CONFIG

# Проверить что переменные очищены
echo "CLAUDE_STATE_DIR: $CLAUDE_STATE_DIR"  # Должно быть пусто
```

### Шаг 2: Выбор метода запуска

#### Метод A: Прямой запуск (для разового использования)
```bash
# Запустить с явным указанием переменной
CLAUDE_STATE_DIR="$HOME/.claude" ./ai-assistant.zsh
```

#### Метод B: Новая shell сессия (рекомендуется для постоянной работы)
```bash
# Начать новую shell сессию
exec zsh

# Теперь переменные будут установлены правильно при первом запуске
./ai-assistant.zsh
```

#### Метод C: Использование тестового скрипта
```bash
# Запустить тестовый скрипт
./test-config.sh
```

### Шаг 3: Проверка конфигурации в контейнере

После запуска Claude в контейнере выполните:

```bash
# 1. Проверить доступность истории чатов
ls -la /root/.claude/history.jsonl

# 2. Проверить размер файла истории
stat /root/.claude/history.jsonl

# 3. Проверить команду resume
claude /resume

# 4. Проверить что настройки загружены
cat /root/.claude/settings.json | head -10
```

### Шаг 4: Валидация успеха

✅ **Признаки успешной конфигурации:**
- Файл `/root/.claude/history.jsonl` существует и имеет размер > 600KB
- Команда `claude /resume` показывает полную историю чатов
- Нет ошибок при запуске контейнера

❌ **Если что-то не работает:**
1. Проверьте права доступа к `~/.claude`
2. Убедитесь что Docker запущен
3. Проверьте что образ `claude-code-tools:latest` доступен

---

## 🔧 Дополнительные команды

### Проверка volume mapping
```bash
# Тестовый запуск контейнера с нашей конфигурацией
docker run --rm \
  -v ~/.claude:/root/.claude \
  -e CLAUDE_CONFIG_DIR=/root/.claude \
  claude-code-tools:latest \
  ls -la /root/.claude/
```

### Создание символической ссылки для удобства
```bash
# Создать символическую ссылку (опционально)
ln -sf "$(pwd)/ai-assistant.zsh" /usr/local/bin/claude-docker

# Теперь можно запускать просто:
claude-docker
```

### Проверка истории чатов
```bash
# Проверить размер файла истории на хосте
ls -lh ~/.claude/history.jsonl

# Проверить количество записей
wc -l ~/.claude/history.jsonl
```

---

## 🛠️ Отладка и решение проблем

### Проблема: Переменные не применяются
**Решение:**
```bash
# Полный сброс и перезапуск
unset CLAUDE_STATE_DIR CLAUDE_CONFIG
exec zsh
./ai-assistant.zsh
```

### Проблема: История не отображается
**Проверка:**
```bash
# Проверить что файл существует
ls -la ~/.claude/history.jsonl

# Проверить права доступа
ls -ld ~/.claude/

# Проверить монтирование в контейнере
docker run --rm -v ~/.claude:/root/.claude claude-code-tools:latest ls /root/.claude/
```

### Проблема: Ошибки при запуске
**Диагностика:**
```bash
# Проверить Docker
docker --version
docker ps

# Проверить образ
docker images | grep claude

# Проверить скрипт на синтаксис
bash -n ./ai-assistant.zsh
```

---

## 🔄 Процедура отката

Если что-то пошло не так:

```bash
# 1. Восстановить backup файлы
rm -rf ~/.claude
mv ~/.claude.backup.TIMESTAMP ~/.claude
cp ~/.claude.json.backup.TIMESTAMP ~/.claude.json

# 2. Восстановить оригинальный скрипт
cp ./ai-assistant.zsh.original ./ai-assistant.zsh

# 3. Удалить символическую ссылку если создавалась
rm -f /usr/local/bin/claude-docker
```

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте логи**: `./ai-assistant.zsh` покажет ошибки при запуске
2. **Запустите тестовый скрипт**: `./test-config.sh`
3. **Проверьте проект ревью**: `cat PROJECT_REVIEW.md`
4. **Ознакомьтесь с планом**: `cat ../.claude/plans/scalable-conjuring-sunbeam.md`

---

## 📊 Краткий чек-лист перед запуском

- [ ] Backup файлы созданы
- [ ] Скрипты скопированы в проект
- [ ] Переменные окружения очищены
- [ ] Docker запущен
- [ ] Образ claude-code-tools доступен
- [ ] `./ai-assistant.zsh` модифицирован

**Готово к запуску!** 🚀