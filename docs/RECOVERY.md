# Recovery Guide: Восстановление Настроек Claude Code

> 📋 **Recovery Guide** | [Home](../README.md) > [Defensive Backup/Restore Plan](./DEFENSIVE_BACKUP_RESTORE_PLAN.md) > **Recovery Procedures**

**Создано**: 2025-12-30
**Обновлено**: 2025-12-30
**Версия**: 1.0

---

## 🎯 Назначение

Данное руководство описывает процедуры ручного восстановления настроек Claude Code (`~/.claude/settings.json`) в случаях, когда автоматический restore механизм не сработал.

---

## 🚨 Когда Использовать

Используйте это руководство, если:
- ✅ Автоматический restore failed (сообщение об ошибке)
- ✅ После выхода из контейнера настройки повреждены
- ✅ Claude Code не запускается (ошибка "invalid JSON")
- ✅ Требуется откатиться к предыдущему состоянию

---

## 📊 Архитектура Backup Системы

### Расположение Backup Файлов

```
~/.claude/
├── settings.json                    ← Текущий config (может быть поврежден)
├── .settings.last_session           ← Последняя успешная сессия
├── .settings.emergency.*            ← Emergency backup (если restore failed)
├── .backups/
│   ├── settings-20251230-120000.json  ← Persistent backup (timestamp)
│   ├── settings-20251230-115500.json
│   └── settings-20251230-115000.json  ← Всего хранится 3 последних
└── .settings.backup.*               ← Temporary backup (обычно удаляется после restore)
```

### Приоритет Восстановления

1. **`.settings.last_session`** - самый свежий, последняя успешная сессия
2. **`.backups/settings-[timestamp].json`** - persistent backups (последние 3)
3. **`.settings.emergency.*`** - если restore failed, это pre-failure state
4. **`.settings.backup.*`** - temporary, если еще не удален

---

## 🔧 Процедура Восстановления

### Шаг 1: Диагностика Проблемы

**Проверьте текущее состояние**:
```bash
# Проверить, существует ли settings.json
ls -lah ~/.claude/settings.json

# Проверить валидность JSON
jq empty ~/.claude/settings.json
# Если ошибка → JSON поврежден
```

**Типичные ошибки**:
```
parse error: Invalid numeric literal at line 1, column 5
→ JSON syntax corrupted

No such file or directory
→ Файл отсутствует полностью
```

---

### Шаг 2: Найти Подходящий Backup

**Проверить последнюю сессию** (рекомендуется):
```bash
ls -lah ~/.claude/.settings.last_session
```

**Проверить persistent backups**:
```bash
# Показать все backups (отсортированы по времени)
ls -lt ~/.claude/.backups/settings-*.json

# Показать с размером файла
ls -lh ~/.claude/.backups/settings-*.json
```

**Проверить emergency backup** (если restore failed):
```bash
ls -lah ~/.claude/.settings.emergency.*
```

**Выбор backup**:
- ✅ Используйте `.settings.last_session` если доступен (самый безопасный)
- ✅ Используйте последний persistent backup из `.backups/`
- ⚠️ Emergency backup используйте только если другие недоступны

---

### Шаг 3: Валидация Backup

**Перед восстановлением проверьте backup**:
```bash
# Заменить BACKUP_FILE на путь к выбранному backup
BACKUP_FILE=~/.claude/.settings.last_session

# Проверить JSON валидность
jq empty "$BACKUP_FILE"
# Если без ошибок → backup корректен

# Показать содержимое (первые 10 строк)
jq . "$BACKUP_FILE" | head -20

# Проверить размер файла (должен быть разумным, не 0)
ls -lh "$BACKUP_FILE"
```

**Критерии валидного backup**:
- ✅ `jq empty` выполняется без ошибок
- ✅ Размер файла > 0 байт (обычно 100-1000 байт)
- ✅ Содержит ключевые поля (например, `ANTHROPIC_BASE_URL`)

---

### Шаг 4: Создать Backup Текущего State (Safety)

**Перед восстановлением сохраните текущий state**:
```bash
# Создать backup текущего (возможно поврежденного) файла
cp ~/.claude/settings.json ~/.claude/settings.json.before-recovery 2>/dev/null || true
```

Это позволит откатиться, если что-то пойдет не так.

---

### Шаг 5: Восстановление

**Метод A: Из .settings.last_session (рекомендуется)**:
```bash
cp ~/.claude/.settings.last_session ~/.claude/settings.json
chmod 600 ~/.claude/settings.json

# Проверить восстановление
jq empty ~/.claude/settings.json && echo "✅ Восстановление успешно"
```

**Метод B: Из persistent backup**:
```bash
# Заменить TIMESTAMP на нужный (например, 20251230-120000)
TIMESTAMP="20251230-120000"
cp ~/.claude/.backups/settings-$TIMESTAMP.json ~/.claude/settings.json
chmod 600 ~/.claude/settings.json

# Проверить восстановление
jq empty ~/.claude/settings.json && echo "✅ Восстановление успешно"
```

**Метод C: Из emergency backup**:
```bash
# Найти emergency файл
EMERGENCY=$(ls -t ~/.claude/.settings.emergency.* 2>/dev/null | head -1)

if [[ -n "$EMERGENCY" ]]; then
    cp "$EMERGENCY" ~/.claude/settings.json
    chmod 600 ~/.claude/settings.json
    echo "✅ Восстановлен из emergency backup: $EMERGENCY"
else
    echo "❌ Emergency backup не найден"
fi
```

---

### Шаг 6: Верификация

**Проверить восстановленные настройки**:
```bash
# 1. JSON валидность
jq empty ~/.claude/settings.json

# 2. Просмотр содержимого
cat ~/.claude/settings.json

# 3. Проверить права доступа (должно быть 600)
ls -l ~/.claude/settings.json
# Ожидается: -rw------- (только owner может читать/писать)

# 4. Попробовать запустить Claude Code
claude-code --version
# Если запускается без ошибок → успех!
```

---

## 🔄 Специальные Сценарии

### Сценарий 1: Все Backups Потеряны

Если все backup файлы отсутствуют:

```bash
# Создать минимальную валидную конфигурацию
cat > ~/.claude/settings.json << 'EOF'
{
  "permissions": {
    "defaultMode": "default"
  }
}
EOF

chmod 600 ~/.claude/settings.json

# После запуска Claude Code настроит остальное автоматически
claude-code
```

---

### Сценарий 2: Восстановление GLM Конфигурации

Если вам нужна GLM конфигурация:

```bash
# Использовать project template
cp ./.claude/settings.template.json ~/.claude/settings.json 2>/dev/null

# Или создать минимальную GLM конфигурацию
cat > ~/.claude/settings.json << 'EOF'
{
  "ANTHROPIC_BASE_URL": "https://api.z.ai/api/anthropic",
  "ANTHROPIC_MODEL": "glm-4.7",
  "ANTHROPIC_HAIKU_MODEL": "glm-4.5-air"
}
EOF

chmod 600 ~/.claude/settings.json
```

**Важно**: Установите `ANTHROPIC_AUTH_TOKEN` как переменную окружения, не в файле.

---

### Сценарий 3: Восстановление Claude Pro Конфигурации

Для Claude Pro (Sonnet):

```bash
cat > ~/.claude/settings.json << 'EOF'
{
  "ANTHROPIC_BASE_URL": "https://api.anthropic.com",
  "ANTHROPIC_MODEL": "claude-sonnet-4.5",
  "permissions": {
    "defaultMode": "default"
  }
}
EOF

chmod 600 ~/.claude/settings.json
```

---

## 📋 Checklist: Post-Recovery

После восстановления проверьте:

- [ ] ✅ `jq empty ~/.claude/settings.json` выполняется без ошибок
- [ ] ✅ Права доступа: `-rw-------` (600)
- [ ] ✅ Claude Code запускается: `claude-code --version`
- [ ] ✅ Можете создать новую сессию
- [ ] ✅ API токены работают (если используете API)

---

## 🆘 Troubleshooting

### Проблема: "Permission denied" при записи

**Решение**:
```bash
# Проверить права на директорию
ls -ld ~/.claude/

# Восстановить права
chmod 755 ~/.claude/
chmod 600 ~/.claude/settings.json
```

---

### Проблема: "Invalid JSON" после восстановления

**Решение**:
```bash
# Найти ошибку в JSON
jq . ~/.claude/settings.json
# Ошибка покажет строку и позицию

# Если файл сильно поврежден, используйте другой backup
# или создайте минимальную конфигурацию (см. Сценарий 1)
```

---

### Проблема: Claude Code не принимает настройки

**Решение**:
```bash
# Очистить кэш Claude Code
rm -rf ~/.claude/.cache/ 2>/dev/null || true
rm -rf ~/.claude/session-env/ 2>/dev/null || true

# Перезапустить
claude-code
```

---

## 📞 Дополнительная Помощь

Если recovery не удался:

1. **Проверьте логи**:
   ```bash
   ls -lt ~/.claude/*.log 2>/dev/null | head -5
   ```

2. **Создайте issue** в репозитории:
   - Опишите проблему
   - Приложите (sanitized) вывод `jq . ~/.claude/settings.json`
   - Укажите версию Claude Code: `claude-code --version`

3. **Резервный вариант**: Полная переустановка настроек
   ```bash
   # ВНИМАНИЕ: Удалит все настройки!
   mv ~/.claude ~/.claude.backup
   mkdir ~/.claude
   chmod 755 ~/.claude

   # Claude Code пересоздаст настройки при первом запуске
   claude-code
   ```

---

## 🔗 Связанные Документы

- **[Defensive Backup/Restore Plan](./DEFENSIVE_BACKUP_RESTORE_PLAN.md)** - Implementation details
- **[P8 UAT Plan](./uat/P8_settings_isolation_uat.md)** - Testing procedures
- **[Usage Guide](./USAGE_GUIDE.md)** - Normal operations

---

## 📝 История Изменений

| Версия | Дата | Изменения |
|--------|------|-----------|
| 1.0 | 2025-12-30 | Initial release - Recovery procedures для defensive backup/restore |

---

**Статус**: ✅ ACTIVE
